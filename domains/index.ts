// TODO move domains to plugins
import { Pilot } from './Pilot';
import { Ability, Weapon, Special } from './Skill';
import { Mech, Vek } from './Unit';
import { Squad } from './Squad';

import { Category, Color, Style, Grade } from './constants';

export const Classes = {
  Pilot,
  Ability,
  Weapon,
  Special,
  Mech,
  Vek,
  Squad
}

export const Constants = {
  Category, Color, Style, Grade
}